import { StyledLoginBanner } from './style';

export const LoginBanner = () => {
  return (
    <StyledLoginBanner>
      <p>You are not logged in, so you can only browse.</p>
    </StyledLoginBanner>
  );
};
